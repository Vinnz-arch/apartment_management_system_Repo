from django.urls import path
from . import views

app_name = 'rental_application'

urlpatterns = [
    # Add your URL patterns here
    # Example:
    # path('', views.index, name='index'),
    # path('apply/', views.apply, name='apply'),
] 